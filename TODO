Some TODOs, in no particular order:

- make the entry points OSGi components
- make the examples runnable through a Gogo shell
- add an OSGi distribution so that the gogo commands are easily runnable
- license checks using maven plugin
- use proper parent pom ( LMN? )